spring:
  application:
    name: API - THE BEST
  datasource:
    url: jdbc:h2:mem:jogadores
    username: sa
    password: 123456
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console